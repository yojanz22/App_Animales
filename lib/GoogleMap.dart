import 'package:flutter/material.dart';

class GoogleMapPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Contenido de Google Map Page'),
    );
  }
}
